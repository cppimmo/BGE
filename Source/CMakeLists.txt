# CMakeList.txt: Top-level CMake project file, do global configuration and include sub-projects here.

cmake_minimum_required(VERSION 3.21)
project(BGE)
# Set the configuration types intended for use with any and all sub-projects:
set(CMAKE_CONFIGURATION_TYPES Debug Release) # Profile
set(CMAKE_DEBUG_POSTFIX "") # Empty CMAKE_DEBUG_POSTFIX
# Set the C++ standard.
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED On)
set(CMAKE_CXX_EXTENSIONS Off)
if(UNIX)
  set(CMAKE_POSITION_INDEPENDENT_CODE On) # Needs to be set on non-Windows for linking static libs to shared
endif()
set(CMAKE_WINDOWS_EXPORT_ALL_SYMBOLS On) # This is needed on Windows to generate a .lib file for shared libs

set_property(GLOBAL PROPERTY USE_FOLDERS ON)

# Add preprocessor defines for the available configurations:
add_compile_definitions($<$<CONFIG:Debug>:BGE_CONFIG_DEBUG>
  $<$<CONFIG:Profile>:BGE_CONFIG_PROFILE>
  $<$<CONFIG:Release>:BGE_CONFIG_RELEASE>)

# Macro creates a list of source file matching expression named LIST with
# SOURCE_DIR prepended to each pattern.
macro(get_sources_match_list LIST SOURCE_DIR)
  file(GLOB_RECURSE ${LIST} LIST_DIRECTORIES true CONFIGURE_DEPENDS
	"${SOURCE_DIR}/*.cpp"
	"${SOURCE_DIR}/*.hpp"
	"${SOURCE_DIR}/*.ipp"
	"${SOURCE_DIR}/*.h")
endmacro()

# Set the output directory for our shared library binaries:
# TARGET_NAME is the name of the target.
# OUTPUT_DIR is the directory stored in an environment variable (most-likely).
macro(set_target_output_directory TARGET_NAME OUTPUT_DIR)
  # The $<0:> removes the Debug & Release folders generated by Visual Studio.
  set_target_properties(${TARGET_NAME} PROPERTIES
	RUNTIME_OUTPUT_DIRECTORY ${OUTPUT_DIR}$<0:>
	LIBRARY_OUTPUT_DIRECTORY ${OUTPUT_DIR}$<0:>
	ARCHIVE_OUTPUT_DIRECTORY ${OUTPUT_DIR}$<0:>)
  # Loop through project-wide configuration type:
  foreach(OUTPUT_CONFIGURATION ${CMAKE_CONFIGURATION_TYPES})
	string(TOUPPER ${OUTPUT_CONFIGURATION} OUTPUT_CONFIGURATION)
	set_target_properties(${TARGET_NAME} PROPERTIES
	  CMAKE_RUNTIME_OUTPUT_DIRECTORY_${OUTPUT_CONFIGURATION} ${OUTPUT_DIR}$<0:>
	  CMAKE_LIBRARY_OUTPUT_DIRECTORY_${OUTPUT_CONFIGURATION} ${OUTPUT_DIR}$<0:>
	  CMAKE_ARCHIVE_OUTPUT_DIRECTORY_${OUTPUT_CONFIGURATION} ${OUTPUT_DIR}$<0:>)
  endforeach()
endmacro()

# Creates directory tree of soure files in soultion explorer menu.
function(create_source_group SRC_FILES)
  #message(STATUS "CMAKE_CURRENT_SOURCE_DIR=${CMAKE_CURRENT_SOURCE_DIR}")
  #message(STATUS "SRC_FILES=${SRC_FILES}")
  source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} PREFIX "Source Files" FILES ${SRC_FILES})
endfunction()

set(LIB_DIR "../Source/Lib")


# SDL library:
set(SDL_VENDORED OFF CACHE BOOL "Use vendored (non-system) version of SDL")
set(SDL_LIB_DIR "Lib/SDL")
# Default to vendored SDL on Windows
if(WIN32)
  set(SDL_VENDORED OFF)
endif()

if(${SDL_VENDORED})
  message(STATUS "Using vendored SDL2")
  # Add the SDL library subdirectory
  add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/Lib/SDL EXCLUDE_FROM_ALL)
  
  # Set include and library directories
  set(SDL2_INCLUDE_DIRS ${CMAKE_CURRENT_SOURCE_DIR}/Lib/SDL/include)
  set(SDL2_LIBRARIES SDL2-static)
  #add_subdirectory(${SDL_LIB_DIR} EXCLUDE_FROM_ALL)
  #set(SDL2_INCLUDE_DIRS "Lib/SDL/include")
  #set(SDL2_INCLUDE_DIRS "Lib/SDL/include/SDL3")
else()
  message(STATUS "Using system-installed SDL2")
  find_package(SDL2 CONFIG REQUIRED)
endif()

#message("SDL2_LIBRARIES: ${SDL_LIBRARIES}")
#message("SDL2_INCLUDE_DIRS: ${SDL_INCLUDE_DIRS}")
include_directories(${SDL2_INCLUDE_DIRS})

# ImGui library:
set(IMGUI_DIR "Lib/imgui")
add_library(ImGui STATIC
  "${IMGUI_DIR}/imgui.cpp"
  "${IMGUI_DIR}/imgui_demo.cpp"
  "${IMGUI_DIR}/imgui_draw.cpp"
  "${IMGUI_DIR}/imgui_tables.cpp"
  "${IMGUI_DIR}/imgui_widgets.cpp"
  "${IMGUI_DIR}/backends/imgui_impl_sdl2.cpp"
  "${IMGUI_DIR}/backends/imgui_impl_opengl3.cpp")
# Set to public so other targets will inherit
target_include_directories(ImGui PUBLIC ${SDL2_INCLUDE_DIRS} ${IMGUI_DIR} "${IMGUI_DIR}/backends")

# ImPlot library:
set(IMPLOT_DIR "Lib/implot")
add_library(ImPlot STATIC
  "${IMPLOT_DIR}/implot.cpp"
  "${IMPLOT_DIR}/implot_demo.cpp"
  "${IMPLOT_DIR}/implot_items.cpp")
target_include_directories(ImPlot PUBLIC ${IMGUI_DIR} ${IMPLOT_DIR})

# FastDelegate library:
set(FASTDELEGATE_DIR "Lib/FastDelegate")
add_library(FastDelegate INTERFACE) # Header only library
target_include_directories(FastDelegate INTERFACE ${FASTDELEGATE_DIR})

# GLM:
set(GLM_DIR "Lib/glm")
add_subdirectory(${GLM_DIR})

# OpenAL-soft library:
set(OPENAL_DIR ${CMAKE_CURRENT_SOURCE_DIR}/Lib/openal-soft)
set(OPENAL_VENDORED OFF CACHE BOOL "Use vendored (non-system) version of OpenAL")

if(WIN32)
  set(OPENAL_VENDORED OFF)
  # Must set the OPENALDIR environment variable on Windows
  #set(ENV{OPENALDIR} "Lib/openal-soft-makemhr/cmake")
  #set(OPENAL_LIBRARY $ENV{OPENALDIR})
  #string(REGEX REPLACE "cmake" "" INC_DIR "$ENV{OPENALDIR}") # "regex" "replacement regex" OUT_VAR INPUT
 # set(OPENAL_INCLUDE_DIR "${INC_DIR}/include")
endif()

if(${OPENAL_VENDORED})
  message(STATUS "Using vendored OpenAL")
  add_subdirectory(${OPENAL_DIR})
else()
  message(STATUS "Using system-installed OpenAL")
  find_package(OpenAL CONFIG REQUIRED)
  include_directories(${OPENAL_INCLUDE_DIRS})
endif()

# glad library:
set(GLAD_DIR "${CMAKE_CURRENT_SOURCE_DIR}/Lib/glad")
add_library(glad STATIC "${GLAD_DIR}/src/gl.c")
target_include_directories(glad PUBLIC "${GLAD_DIR}/include")

# tinyxml2 library:
set(TINYXML_DIR "Lib/tinyxml2")
add_library(TinyXml2 STATIC "${TINYXML_DIR}/tinyxml2.cpp")
target_include_directories(TinyXml2 PUBLIC ${TINYXML_DIR})

# stb library:
set(STB_DIR "Lib/stb")
include_directories(${STB_DIR})

include(FetchContent)

# zlib library:
set(ZLIB_VENDORED OFF CACHE BOOL "Use vendored (non-system) version of zlib")
#set(ZLIB_DIR ${CMAKE_CURRENT_SOURCE_DIR}/Lib/zlib)
#FetchContent_Declare(
#    zlib-cmake
#    URL https://github.com/jimmy-park/zlib-cmake/archive/main.tar.gz
#)
#find_package(ZLIB)
#FetchContent_MakeAvailable(zlib-cmake)
# Default to vendored zlib on Windows
if(WIN32)
  set(ZLIB_VENDORED OFF)
endif()

if(${ZLIB_VENDORED})
  message(STATUS "Using vendored zlib")
  # Add the zlib library subdirectory
  add_subdirectory(${ZLIB_DIR} EXCLUDE_FROM_ALL)
  set(ZLIB_FOUND TRUE)
  set(ZLIB_INCLUDE_DIR "${ZLIB_DIR}/include")
  set(ZLIB_LIBRARIES zlibstatic)
else()
  message(STATUS "Using system-installed zlib")
  #set(ZLIB_ROOT ${ZLIB_DIR})
  #set(ZLIB_INCLUDE_DIR ${ZLIB_DIR})
  #set(ZLIB_LIBRARY ${ZLIB_DIR}/build/Release/zlibstatic.lib)
  #include_directories(${ZLIB_INCLUDE_DIR})
  #target_link_libraries(your_target ${ZLIB_LIBRARY})

  find_package(ZLIB)
endif()

# Pass ZLIB variables to Assimp
#set(ASSIMP_INSTALL OFF)
#set(ASSIMP_BUILD_ZLIB ON) # Disable Assimp's zlib
#set(ZLIB_FOUND ${ZLIB_FOUND} CACHE BOOL "Use external ZLIB")
#set(ZLIB_INCLUDE_DIR ${ZLIB_INCLUDE_DIR} CACHE PATH "ZLIB include directory")
#set(ZLIB_LIBRARIES ${ZLIB_LIBRARIES} CACHE STRING "ZLIB libraries")
# assimp library:
set(ASSIMP_DIR "Lib/assimp")
set(ASSIMP_VENDORED ON CACHE BOOL "Use vendored (non-system version of Assimp")
set(ASSIMP_BUILD_ZLIB OFF)
set(ZLIB_FOUND FALSE)

if(WIN32)
  set(ASSIMP_VENDORED OFF)
endif()

if(${ASSIMP_VENDORED})
  message(STATUS "Using vendored Assimp")
  add_subdirectory(${ASSIMP_DIR})
else()
  message(STATUS "Using system-installed Assimp")
  find_package(assimp CONFIG REQUIRED)
endif()

# See: https://github.com/eliasdaler/cmake-lua-with-sol2-example/
set(LUA_VENDORED OFF CACHE BOOL "Use vendored (non-system) version of Lua")
set(LUA_DIR ${CMAKE_CURRENT_SOURCE_DIR}/Lib/lua)
# Default to vendored Lua on Windows
if(WIN32)
  set(LUA_VENDORED ON)
endif()

if(${LUA_VENDORED})
  message(STATUS "Using vendored Lua")
  add_subdirectory(${LUA_DIR})
  #add_definitions(-DLUA)
  set(LUA_LIBRARY lua_static)
else()
  message(STATUS "Using system-installed Lua")
  find_package(Lua 5.3 REQUIRED)
  include_directories(${LUA_INCLUDE_DIR})
  add_definitions(-DLUA)
endif()
### sol2
set(SOL2_DIR "Lib/sol2")
set(SOL2_LUA_VERSION "5.3.4" CACHE STRING "The version of Lua used")
# we build Lua manually
set(SOL2_BUILD_LUA FALSE CACHE BOOL "Always build Lua, do not search for it in the system")
add_subdirectory(${SOL2_DIR})

# Ogg:
set(OGG_DIR ${CMAKE_CURRENT_SOURCE_DIR}/Lib/ogg)
add_subdirectory(${OGG_DIR})

# Let Vorbis know where to find Ogg
#set(OGG_INCLUDE_DIR ${OGG_DIR}/include)
#set(OGG_LIBRARY ogg) # The target name from Ogg's CMakeLists

# Vorbis:
set(VORBIS_DIR "Lib/vorbis")
add_subdirectory(${VORBIS_DIR})
#find_package(Vorbis REQUIRED) # This will look for libvorbis
#find_package(VorbisFile REQUIRED) # This will look for libvorbisfile

# nlohmann/json:
set(JSON_DIR "Lib/json")
set(JSON_BuildTests OFF CACHE INTERNAL "")
set(JSON_MultipleHeaders ON CACHE INTERNAL "")
add_subdirectory(${JSON_DIR})

find_package(OpenGL REQUIRED)

# FreeType:
#set(FREETYPE_DIR ${CMAKE_CURRENT_SOURCE_DIR}/Lib/freetype)
#add_subdirectory(${FREETYPE_DIR})

# RmlUi:
#set(RMLUI_DIR "Lib/RmlUi")
#add_subdirectory(${RMLUI_DIR})
#include_directories("${RMLUI_DIR}/Include")

# Engine Project:
set(ENGINE_SRC_DIR "BGE")

add_library(Engine SHARED)

target_compile_definitions(Engine PRIVATE BGE_ENGINE_EXPORTS)
target_include_directories(Engine PUBLIC
  "${ENGINE_SRC_DIR}" ${OPENAL_INCLUDE_DIR}
  "${VORBIS_DIR}/include"
) #"${Boost_INCLUDE_DIRS}")

get_sources_match_list(ENGINE_SRC_FILES "${ENGINE_SRC_DIR}")
source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} PREFIX "Source Files" FILES ${ENGINE_SRC_FILES})
target_sources(Engine PRIVATE ${ENGINE_SRC_FILES})

if(WIN32)
  target_compile_options(Engine PUBLIC "/MP")
endif()

target_link_libraries(Engine PUBLIC
  glad
  ${SDL2_LIBRARIES}
  glm::glm
  ImGui ImPlot
  #RmlUi::RmlUi
  assimp::assimp

  TinyXml2
  nlohmann_json::nlohmann_json
  sol2::sol2 

  ${ZLIB_LIBRARY} #ZLIB::ZLIB

  OpenAL::OpenAL
  vorbis
  vorbisfile
  
  FastDelegate

  ${LUA_LIBRARY}
)

if(WIN32)
  # Set the CWD for the debugger to start the program
  set_target_properties(Engine PROPERTIES VS_DEBUGGER_WORKING_DIRECTORY $<TARGET_FILE_DIR:Engine>)
endif()

#if(WIN32)
#  add_custom_command(TARGET Engine POST_BUILD
#	COMMAND ${CMAKE_COMMAND} -E copy_if_different
#   "${LIB_DIR}/SDL2-2.26.3/lib/x64/SDL2.dll" $<TARGET_FILE_DIR:Engine>)
#endif()

# Editor Projects:
set(EDITOR_SRC_DIR "BGEEditor")

add_library(EditorLib SHARED)

target_include_directories(EditorLib PUBLIC "${EDITOR_SRC_DIR}/EditorLib")

get_sources_match_list(EDITOR_LIB_SRC_FILES "${EDITOR_SRC_DIR}/EditorLib")
source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} PREFIX "Source Files" FILES ${EDITOR_LIB_SRC_FILES})
target_sources(EditorLib PRIVATE ${EDITOR_LIB_SRC_FILES})

target_link_libraries(EditorLib Engine)

add_executable(TestGameEditorApp)

target_include_directories(TestGameEditorApp PUBLIC "${EDITOR_SRC_DIR}/EditorApp")

get_sources_match_list(EDITOR_APP_SRC_FILES "${EDITOR_SRC_DIR}/EditorApp")
source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} PREFIX "Source Files" FILES ${EDITOR_APP_SRC_FILES})
target_sources(TestGameEditorApp PRIVATE ${EDITOR_APP_SRC_FILES})

target_link_libraries(TestGameEditorApp Engine EditorLib)

# Game Projects:
set(GAME_SRC_DIR "TestGame")

add_executable(Game)

target_include_directories(Game PUBLIC "${GAME_SRC_DIR}")

get_sources_match_list(GAME_SRC_FILES "${GAME_SRC_DIR}")
source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} PREFIX "Source Files" FILES ${GAME_SRC_FILES})
target_sources(Game PRIVATE ${GAME_SRC_FILES})

target_link_libraries(Game Engine)

if(WIN32)
  # Set the CWD for the debugger to start the program
  set_target_properties(Game PROPERTIES VS_DEBUGGER_WORKING_DIRECTORY $<TARGET_FILE_DIR:Game>)
endif()

add_custom_command(
  TARGET Game
  POST_BUILD
  COMMAND ${CMAKE_COMMAND} -E make_directory "$<TARGET_FILE_DIR:Game>"
  COMMAND ${CMAKE_COMMAND} -E copy_if_different
  "${CMAKE_CURRENT_SOURCE_DIR}/../Game/Engine.xml" "$<TARGET_FILE_DIR:Game>"
  "${CMAKE_CURRENT_SOURCE_DIR}/../Game/Logging.xml" "$<TARGET_FILE_DIR:Game>"
  "${CMAKE_CURRENT_SOURCE_DIR}/../Game/game_icon.bmp" "$<TARGET_FILE_DIR:Game>"
  COMMENT "Copy engine configuration"
)

add_custom_command(
  TARGET Game
  POST_BUILD
  COMMAND ${CMAKE_COMMAND} -E make_directory "$<TARGET_FILE_DIR:Game>/Assets"
  COMMAND ${CMAKE_COMMAND} -E copy_directory_if_different "${CMAKE_CURRENT_SOURCE_DIR}/../Assets/" "$<TARGET_FILE_DIR:Game>/Assets"
  COMMENT "Copy Assets directory"
)
